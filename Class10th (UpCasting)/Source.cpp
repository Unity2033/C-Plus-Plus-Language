#include <iostream>
#include "../Program/Juggling.h"
#include "../Program/Hydra.h"
#include "../Program/Lurker.h"
#include "../Program/Beacon.h"

using namespace std;

int main()
{
#pragma region 업 캐스팅

	// Beacon beacon;
	// 
	// Zerg * juggling = new Juggling();
	// Zerg * hydra = new Hydra();
	// Zerg * lurker = new Lurker();
	// 
	// juggling->SetHP(10);
	// hydra->SetHP(15);
	// lurker->SetHP(20);
	// 
	// cout << "juggling의 현재 체력 : " << juggling->GetHP() << endl;
	// cout << "hydra의 현재 체력 : " << hydra->GetHP() << endl;
	// cout << "lurker의 현재 체력 : " << lurker->GetHP() << endl;
	// 
	// beacon.Trigger(juggling);
	// beacon.Trigger(hydra);
	// beacon.Trigger(lurker);
	// 
	// cout << "juggling의 현재 체력 : " << juggling->GetHP() << endl;
	// cout << "hydra의 현재 체력 : " << hydra->GetHP() << endl;
	// cout << "lurker의 현재 체력 : " << lurker->GetHP() << endl;

#pragma endregion


	return 0;
}
